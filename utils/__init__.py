"""
AugUndo utility package root.

Having this file ensures that ``utils`` is treated as a proper Python package
so imports like ``from utils.src.transforms import Transforms`` work when
``augundo-ext`` is on ``sys.path``.
"""

